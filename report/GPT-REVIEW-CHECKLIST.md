# GPT 审阅意见 → 可核对清单

写这份清单是为了**逐条验收**，不是为了转述。落地后每条都要在报告里找到证据，
**找不到就算没做**（不能靠 agent 说"做了"）。

---

## A. 题目点名技术：必须正面回应

| # | 要求 | 判据 |
|---|---|---|
| A1 | 军师/陪练/老师/Agentic RL/RAG/ReAct/Memory Stream/Reflection **都出现且被正面回应** | 每个词出现处**不止罗列**，有落地说明 |
| A2 | 每个术语**第一次出现**用一句正常中文说明"在本项目里指什么" | 不是英文缩写直译 |
| A3 | **ReAct** = 受控工具调用；**不写"模型思维链"** | 报告里搜不到"思维链/chain of thought" |
| A4 | ReAct **如实写**：真实模型评测中未观察到多工具链 | 出现 44 条 `{0:36, 1:8}` 或等价表述 |
| A5 | **RAG** = 检索本地规则与战术知识，**不负责算当前局面事实** | 有这句区分 |
| A6 | **Memory Stream / Reflection**：**明确写"没有做 LLM 自由总结式 Reflection"** | 有一句明确的"没做" |
| A7 | **Agentic RL**：**明确"没有训练线上 DeepSeek""没有证明真人收益"** | 两句都在 |
| A8 | 四项技术**不是只在一张表里各出现 2 次** | 每项有独立段落展开 |

## B. 语言：不加术语、不包装

| # | 要求 | 判据 |
|---|---|---|
| B1 | 不出现 `deterministic core`/`grounded affect`/`arbitration`/`intervention policy`/`claim discipline`/`hindsight leakage`/`prompted success`/`unprompted success`/`bounded agentic loop`/`temporal leakage`/`measurement correctness` | 全库 grep = 0 |
| B2 | 不把普通设计包装成高级概念（"多智能体角色仲裁"/"grounded affect framework"）| grep = 0 |
| B3 | 不出现「创新地/首次/全面/领先/先进/极大提升/显著提升」 | grep = 0（除非有数据） |
| B4 | 不是每段都以「我实现了/我设计了」开头 | 抽查 |
| B5 | 普通中文能说清就用中文（例："复盘不能偷看后来才发生的事情"）| 抽查 |

## C. 事实表述：强结论必须准确

| # | 要求 | 判据 |
|---|---|---|
| C1 | **「模型只负责措辞」已改**——模型还负责理解开放问题、判断角色、选择查什么、组织回答 | 该句不在；有正确表述 |
| C2 | **「三种角色共用同一条执行链」已改**——不是所有输出都过 LLM（本地模板/模型未接/失败退回/陪练部分主动句）| 有四种路径的说明 |
| C3 | 「游戏本体很简单」→「游戏环境只做到足以产生真实选择」 | 改后措辞在 |
| C4 | 军师条位置描述准确（战斗区顶部，不是页面顶部）| 抽查 |
| C5 | 「全部/都是/共用/只/一定/完整/已实现/学会/胜率/无第三方依赖」逐条成立 | 逐句核 |

## D. 结构

| # | 要求 | 判据 |
|---|---|---|
| D1 | **开头有简述段**（约 180–250 字），回答：做了什么/为什么不是聊天机器人/三角色各解决什么/LLM 与规则引擎各在哪/最大限制 | 存在，且**不叫"摘要"**、不占小节号 |
| D2 | 摘要**不写"模型只负责润色"** | grep 不到 |
| D3 | **工具那节**：为什么需要工具、什么时候调（寒暄不调、要事实先查） | 存在 |
| D4 | 工具清单准确（**8 个**：read_state/search_rules/compare_actions/simulate_branch/inspect_training/read_match/read_evidence/read_last_turn）| 抽查 |
| D5 | **三个角色各用四个问题讲**：何时出现/依据什么/LLM 做什么程序做什么/怎么防越界 | 三节都有 |
| D6 | **老师**：复盘不偷看未来（用回合开始前的局面）+ **被提示做对 ≠ 学会**（区分有无提示的行为）| 两个都在 |
| D7 | 第 3 章改成「设计与实现中的关键难点」，**不以 coding agent 为主线** | 标题 + 内容 |
| D8 | 难点 5–6 项，含：LLM 不能自己算游戏 / 复盘不能知道未来 / 主动提醒容易变烦 / 被提示做对≠学会 / 检索到了≠答案对 / 测试过≠功能好 | 抽查 |
| D9 | **AI coding agent 压成文末 2–3 句**，不再整章 | 文末存在 |
| D10 | **第 5 章验证表**：问题/方法/当前结果/能说明什么/不能说明什么 | ✅ **已改为两张三列表**（五字段分居两表表头）；一张五列表在 453pt 下每列仅 80pt、首列被挤成竖条 |
| D11 | **未验证清单**（真人觉得自然/是否提高胜率/长期迁移/提示频率/联网 PVP/权威状态/语音）| 存在 |
| D12 | 不新增"摘要"章节标题（用户说算了；但要有 D1 那段）| 抽查 |

## E. 数字与验证

| # | 要求 | 判据 |
|---|---|---|
| E1 | 所有数字来自 current master / 当前产物 | 抽样核对 `reports/` |
| E2 | 宠物 14 / 属性 7 / 关卡 5 / 知识卡 93 | 实测 |
| E3 | 测试数 = 当前实际 | `npm test` |
| E4 | RAG 如实：**若 MRR 提高但 top-3 未增加，写"只能说明排序改善"** | 该措辞在 |
| E5 | 真实模型评测如实 | 读 `reports/` |
| E6 | RL 实验**主动讲 reward 设计有风险**（玩家本来会做对的不算 AI 收益）；**提示变少同时帮助也下降要一起展示** | 抽查 |
| E7 | 浏览器测试写成"浏览器自动冒烟测试"，**不写成"用户测试"** | grep |

## F. 视觉

| # | 要求 | 判据 |
|---|---|---|
| F1 | 9–11 页 | 渲染计数 |
| F2 | 无 overfull / underfull / undefined | 编译日志 |
| F3 | 图不缩小到不可读（≥7pt）| 抽查 |
| F4 | 无重复内容、无越界表格、无前后位置冲突 | 逐页看 |
| F5 | 封面：居中、无句号、上下两根短线、`\small` 黑色 | **已完成，勿动** |

## G. 封面（**已完成，验收时确认没被改回去**）

```latex
\begin{center}
\vspace{0.45em}
\rule{0.4\linewidth}{0.4pt}\\[0.55em]
{\small 本报告由 AI 辅助撰写}\\[0.55em]
\rule{0.4\linewidth}{0.4pt}
\end{center}
```

---

## 审计方法

**不靠 agent 的自述**。每条：
- 能在报告里**指出原文**
- 或能用 `grep` / `npm test` / 渲染**机械验证**
- **做不到就算未通过**

## 已记录在案的既有错误（防止回归）

1. `\shotH` 未定义（`figs/hfig.tex` 无引用）→ 编译断
2. `fig:strategist-hint.png` undefined reference
3. 曾出现同一张图被编译两遍（`00-pipeline.tex` + `02-delivery.tex` 各一份）
4. 曾出现把 `\input` 加回去导致重复
