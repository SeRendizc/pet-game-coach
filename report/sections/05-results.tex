\section{交付结果}

结果就是小芽本身：一个住在游戏里的 AI Coach。一局游戏从头到尾，玩家不打开聊天面板也会被它接住；三种角色各自跑完了自己的闭环——军师在局面里给可解释的取舍，老师在一局结束后复盘并把课上下去，陪练在左下角陪着，记得住、有情绪、不越界。

\textbf{军师：每一步的取舍都算过，也说得出依据。} 轮到玩家出招时，引擎已经把双方所有合法行动枚举过一遍，各是什么后果都算过；模型拿到这堆事实，决定这笔取舍怎么讲。点开「看看原因」，展开的就是它引用的血量、能量和知识卡，每张卡还带着它自己的反例。它给的是眼下这一步划不划算，不是整局的胜率，也不替玩家出招。它也知道什么时候该闭嘴：安静档下只在被问时回答；主动开口只留给几种固定局面——伙伴倒下要补位、来回换手、明显是错的一手、停在一个不划算的选项上；每局最多开口三次，每种局面各一次，两次之间至少隔一分钟，点掉任意一条就本局静音。

\textbf{老师：复盘、讲解、小测，还带一本教学账本。} 一局打完，它先给一句话结论，再从减员和生命变化里挑三个回合逐个讲：当时双方还剩多少血、这一手实际打出了什么、当时还能选哪些做法、比实际这一手好多少。讲评只用回合开始时的公开状态，不拿对手打出的那一招当理由——否则每次失败都能说成「你当时选错了」。局中它盯着玩家正在看的那个技能讲，讲完那句是「出不出它由你决定」，不催出招。小测按玩家自己的面板出变式题，选项里带「不确定」。要不要再讲一遍只看有没有新的反证：讲过又没有就不重复，只有军师独立看到三次同类失误才重新开课，并说得出是哪三次。教学账本只数没被提示过的行动：判断一个人有没有学会，看他没人提示时怎么选。

\textbf{陪练：记得住、有情绪、不越界。} 它在战斗页左下角，一个独立气泡，不打开聊天也看得见；点开就是聊天，玩家只说「你好」它也接得住。它说的话都要落在一件玩家自己算不出来的事上：这套阵容打过几次、上一局停在第几回合、最近几局最先倒下的总是哪一只、上次来是几天前、对面这一局的伤害有多少落在同一只身上。情绪必须落在真实事件上：能说的只有可惜、漂亮、悬、憋屈、松口气五个词，每一句都绑在刚结算的那一手上，说清是第几回合、哪一只、哪一记技能；它不说自己的感受，「我看得有点急」这种句子会被拦掉。它记得住连败、药还剩几瓶、本命是哪只、上次玩是几天前，还有玩家声明过的偏好和目标，据此决定语气档位——连着输了两三局，别的话就收起来，只陪着坐一会儿。边界是写死的：不评价玩家水平、不给战术指令（那是军师的事）、不说教；每局主动搭话有上限，最近七天被点掉的次数还会把上限压低。

模型没接上、超时、输出过长，或者和工具回执对不上时，规则建议、复盘、培养建议和小测仍由本机引擎给，屏幕上显示的是本机已核验的结论，并说明为什么退回。

\subsection{这些结论各自证明了什么}

上面这些说法背后是五组跑出来的结果。表~\ref{tab:what-ran} 是每组问题用的方法和跑出来的数，表~\ref{tab:what-proves} 是同一批结果能说明什么、不能说明什么。

% 五列在 453pt 栏宽里放不下：分掉列间距，每列只剩 80 来 pt，一个单元格要折成四到五行，
% 第一列更是被挤成两三个字的竖条。所以按「做了什么」和「能说明什么」拆成两张三列表，
% 每张只剩标签一列加两个正文列，单元格宽度翻倍，折行降到两三行。
%
% 列宽算法：正文栏宽 453.5pt，tabcolsep 6pt × 6 = 36pt 是列间距，剩下约 417pt 分给三列——
% 标签 1.6cm（四个汉字一行放得下），两个正文列各 6.5cm。1.6 + 6.5 + 6.5 = 14.6cm = 414pt，
% 加列间距 450pt，离栏宽还差 3pt，不会 overfull。
%
% 标签列用 array 宏包自带的 m{} 上下居中，正对着右侧那两三行；正文列仍用 preamble 里的
% L{}（p{} 加 \raggedright）顶部对齐——多行单元格首行齐平，比整体居中更好读。
% \addlinespace 给行与行之间留出可读的间距，booktabs 三条横线划出表头与表尾。
%
% 数字与措辞照抄旧表，只把第一列的短标签提出来、把问题原文挪进「方法」列。
%
% 每张表连着它上面那行「表 N」标题一起塞进一个 minipage：minipage 是一个不可分割的
% 盒子，标题就不会被留在上一页页脚、表格自己跑到下一页去（第一版渲染时表 2 的标题
% 正是这么被孤立的）。两张表各自成盒，页与页之间仍然可以正常断开。
{\footnotesize
\newcommand{\tabhead}[2]{\refstepcounter{table}\textbf{表~\thetable}\quad #2\label{#1}\par}
\noindent\begin{minipage}{\linewidth}
\tabhead{tab:what-ran}{每组问题用的方法和跑出来的数}
\vspace{2pt}
\centering
\begin{tabular}{>{\raggedright\arraybackslash}m{1.6cm} L{6.5cm} L{6.5cm}}
\toprule
\textbf{问题} & \textbf{方法} & \textbf{当前结果} \\
\midrule
检索 & 该用哪条路：四臂对照（不检索 / 词项 / 混合 / 反例加条件），同一份 128 条查询 & 前 3 命中率 0 / 0.884 / 0.902 / 0.911；命中正确卡的条数 0 / 99 / 101 / 102 \\
\addlinespace[4pt]
工具调用 & 模型真的会连查几次：44 条真实模型记录，逐条统计调用次数与停止原因 & 调用 0 次 36 条、1 次 8 条；1 条参数非法、1 条重复被拦；没有多步工具链 \\
\addlinespace[4pt]
主动提醒 & 该在什么时候开口：离线策略对照（固定规则 / 随机初始化 / Q-learning），含行为分布变化的一档 & 奖励 0 / $-1.113$ / $-0.669$ / $1.012$（变化时 0 / $-2.33$ / $-1.679$ / $0.581$） \\
\addlinespace[4pt]
小模型工具 & 能不能学会选工具：SmolLM2-135M 冻结骨干，只训两个工具输出行共 1152 个参数，24 训练 / 8 验证 / 16 测试 & 未训练 16 条全选同一工具，准确率 0.50；训练后 0.9375，三种子一致 \\
\addlinespace[4pt]
回归 & 改完有没有碰坏别的：\code{npm test} 全量跑；无头浏览器冒烟：从营地选人、出招打到结算 & 362 项全通过，0 失败；冒烟走完整局无报错 \\
\bottomrule
\end{tabular}
\par
\end{minipage}
\par\vspace{8pt}
\noindent\begin{minipage}{\linewidth}
\tabhead{tab:what-proves}{同一批结果能说明什么、不能说明什么}
\vspace{2pt}
\centering
\begin{tabular}{>{\raggedright\arraybackslash}m{1.6cm} L{6.5cm} L{6.5cm}}
\toprule
\textbf{问题} & \textbf{能说明什么} & \textbf{不能说明什么} \\
\midrule
检索 & 词项检索有效；反例加权比词项高 2.68 个百分点；混合与词项分不出来 & 量的只是「正确卡进没进前 3」，说明排序有改善，说明不了最终回答质量 \\
\addlinespace[4pt]
工具调用 & 循环在真实调用里稳定，参数校验与停止条件会触发 & 不能说明模型已在用多步工具链，上限靠确定性测试覆盖 \\
\addlinespace[4pt]
主动提醒 & 在模拟环境里，学到的时机比「到点就提醒」省打扰 & 奖励函数和我自己写的模拟器就是全部环境，没有真人 \\
\addlinespace[4pt]
小模型工具 & 在这个二选一的小任务上，这 1152 个参数确实学到了东西 & 手写的小型选择场景，不是多步 Agent 训练，也没微调线上 DeepSeek \\
\addlinespace[4pt]
回归 & 已知行为没被改坏，玩家会走的那条路是通的 & 测试只数它被要求数的东西；框架本身也出过错（见第 3 章） \\
\bottomrule
\end{tabular}
\par
\end{minipage}
\par
}

还没做到的地方：语音没做，联网对战是同机分屏，仓库里的演示是无头浏览器抓的截图，不是真人录屏；玩家是不是真的学会了，要等真人用一遍、把提示撤掉之后还能自己做对，才算数。
